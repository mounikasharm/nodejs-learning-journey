# Chapter 03: Writing Your First Node.js Script
Node.js allows us to run JavaScript code outside the browser.
In this chapter, we will write our first Node.js script.
```bash
node -v
```
👉 Output:
node -v
## Checking Node.js Version

If Node.js is installed, check the version using the command below:

```bash
node -v
This command displays the installed Node.js version.
---

### 5️⃣ Example: JavaScript Code Block

```md
## First JavaScript in Node.js

Create a file named `index.js` and add the following code:

```js
console.log("Hello World");
Run the file using:node index.js

---

### 6️⃣ Use **inline code** for small commands

```md
Use `node index.js` to run the file.
